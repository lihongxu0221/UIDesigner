namespace BgControls.Tools.Converter;/// <summary>/// 反向布尔值到可见性枚举的转换器 (Boolean Inverse to Visibility)./// </summary>/// <remarks>/// <para>/// 此转换器将 <see cref="bool"/> 值转换为 <see cref="Visibility"/> 枚举值，但逻辑与标准的 BooleanToVisibilityConverter 相反./// </para>/// <para>/// <b>转换逻辑：</b>/// <list type="bullet">/// <item><c>true</c> -> <see cref="Visibility.Collapsed"/> (隐藏)</item>/// <item><c>false</c> -> <see cref="Visibility.Visible"/> (显示)</item>/// <item><c>null</c> -> <see cref="Visibility.Visible"/> (显示)</item>/// </list>/// </para>/// </remarks>[Obsolete("建议使用 BgControls.Tools.Converter.Equality2ValueConverter 替代此转换器。")]public class Boolean2VisibilityReConverter : IValueConverter{    /// <summary>    /// 将布尔值转换为反向的可见性状态.    /// </summary>    /// <param name="value">绑定的源值 (通常为 <see cref="bool"/>).</param>    /// <param name="targetType">绑定目标的类型 (通常为 <see cref="Visibility"/>).</param>    /// <param name="parameter">转换器参数 (未使用).</param>    /// <param name="culture">区域信息.</param>    /// <returns>    /// 如果 <paramref name="value"/> 为 <c>true</c>，返回 <see cref="Visibility.Collapsed"/>；    /// 否则返回 <see cref="Visibility.Visible"/>.    /// </returns>    public object Convert(object value, Type targetType, object parameter, CultureInfo culture)    {        // 使用模式匹配 (is) 替代强制转换，更加安全        // 逻辑：只有当 value 是 bool 且为 true 时，才隐藏        if (value is bool booleanValue && booleanValue)        {            return Visibility.Collapsed;        }        return Visibility.Visible;    }    /// <summary>    /// 反向转换方法 (不支持).    /// </summary>    /// <param name="value">目标属性的值.</param>    /// <param name="targetType">源数据的类型.</param>    /// <param name="parameter">转换器参数.</param>    /// <param name="culture">区域信息.</param>    /// <returns>不返回任何值，总是抛出异常.</returns>    /// <exception cref="NotSupportedException">此转换器仅用于单向绑定，不支持双向转换.</exception>    public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)    {        throw new NotSupportedException();    }}