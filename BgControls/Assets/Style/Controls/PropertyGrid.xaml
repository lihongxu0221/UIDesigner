<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"                    xmlns:hc="https://handyorg.github.io/handycontrol"                    xmlns:local="clr-namespace:BgControls.Windows.Controls"                     xmlns:attach="clr-namespace:BgControls.Windows.Attach"                     xmlns:markup="clr-namespace:BgControls.Windows.Markup"                    xmlns:prim="clr-namespace:BgControls.Windows.Primitives"                     xmlns:pg="clr-namespace:BgControls.Windows.Controls.PropertyGrid"                     xmlns:commands="clr-namespace:BgControls.Windows.Controls.PropertyGrid.Commands"                    xmlns:editor="clr-namespace:BgControls.Windows.Controls.PropertyGrid.Editors"                     xmlns:utilities="clr-namespace:BgControls.Core.Utilities"                     xmlns:conv="clr-namespace:BgControls.Tools.Converter"                    xmlns:toolConverters="clr-namespace:BgControls.Tools.Converter"                    xmlns:pgconv="clr-namespace:BgControls.Windows.Controls.PropertyGrid.Converters"                    xmlns:tool="clr-namespace:BgControls.Tools"                    xmlns:sys="clr-namespace:System;assembly=mscorlib">    <ResourceDictionary.MergedDictionaries>        <tool:SharedResourceDictionary Source="/BgControls;component/Assets/Style/Control.xaml" />    </ResourceDictionary.MergedDictionaries>    <sys:Double x:Key="DefaultPropertyItemHeight">32</sys:Double>    <toolConverters:Null2BoolConverter x:Key="Null2BoolConverter" />    <pgconv:ExpandableObjectMarginConverter x:Key="ExpandableObjectMarginConverter" />    <pgconv:ObjectToUIElementConverter x:Key="ObjectToUIElementConverter" />    <pgconv:IsStringEmptyConverter x:Key="IsStringEmptyConverter" />    <pgconv:IsDefaultCategoryConverter x:Key="IsDefaultCategoryConverter" />    <pgconv:SelectedObjectConverter x:Key="objectConverter" />    <pgconv:PropertyItemEditorConverter x:Key="PropertyItemEditorConverter" />    <pgconv:PropertyNameTextTrimmingConverter x:Key="PropertyNameTextTrimmingConverter" />    <pgconv:NewItemTypesComboBoxConverter x:Key="NewItemTypesComboBoxConverter" />    <pgconv:IsCustomPropertyItemConverter x:Key="IsCustomPropertyItemConverter" />    <pgconv:CategoryExpanderConverter x:Key="CategoryExpanderConverter" />    <SolidColorBrush x:Key="MouseOverBorderBrush" Color="#FFFFB700" />    <LinearGradientBrush x:Key="MouseOverBackgroundBrush" StartPoint="0,0" EndPoint="0,1">        <GradientStop Offset="0" Color="#FFFEFBF4" />        <GradientStop Offset="0.19" Color="#FFFDE7CE" />        <GradientStop Offset="0.39" Color="#FFFDDEB8" />        <GradientStop Offset="0.39" Color="#FFFFCE6B" />        <GradientStop Offset="0.79" Color="#FFFFDE9A" />        <GradientStop Offset="1" Color="#FFFFEBAA" />    </LinearGradientBrush>    <SolidColorBrush x:Key="CheckedBorderBrush" Color="#FFC29B29" />    <LinearGradientBrush x:Key="CheckedBackgroundBrush" StartPoint="0,0" EndPoint="0,1">        <GradientStop Offset="0" Color="#FFFFDCA0" />        <GradientStop Offset="0.18" Color="#FFFFD692" />        <GradientStop Offset="0.39" Color="#FFFFC45D" />        <GradientStop Offset="1" Color="#FFFFD178" />    </LinearGradientBrush>    <SolidColorBrush x:Key="PressedBorderBrush" Color="#FFC29B29" />    <LinearGradientBrush x:Key="PressedBackgroundBrush" StartPoint="0,0" EndPoint="0,1">        <GradientStop Offset="0" Color="#FFE3C085" />        <GradientStop Offset="0.19" Color="#FFF4CC89" />        <GradientStop Offset="0.36" Color="#FFF5C777" />        <GradientStop Offset="0.36" Color="#FFF5BB56" />        <GradientStop Offset="0.79" Color="#FFF4CE9A" />        <GradientStop Offset="1" Color="#FFF3E28D" />    </LinearGradientBrush>    <!--<Style x:Key="PropertyGridNumericUpDown" TargetType="{x:Type prim:InputBase}" BasedOn="{StaticResource NumericUpDown}">        <Setter Property="prim:InputBase.TextAlignment" Value="Left" />        <Setter Property="AutomationProperties.AutomationId" Value="PropertyItemEditor" />    </Style>-->    <!--PropertyGrid NumberInput Editors-->    <Style TargetType="{x:Type editor:PropertyGridEditorNumberInput}" BasedOn="{StaticResource InputElementBaseStyle}">        <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />        <Setter Property="IsTabStop" Value="False" />        <Setter Property="AllowDrop" Value="true" />        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst" />        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />        <Setter Property="Background" Value="Transparent" />        <Setter Property="BorderBrush" Value="Transparent" />        <Setter Property="BorderThickness" Value="0" />        <Setter Property="HorizontalContentAlignment" Value="Left" />        <Setter Property="VerticalContentAlignment" Value="Center" />        <Setter Property="DecimalPlaces" Value="0" />        <Setter Property="Value" Value="0" />        <Setter Property="Margin" Value="0" />        <Setter Property="attach:BorderElement.CornerRadius" Value="{DynamicResource DefaultCornerRadius}" />        <Setter Property="FontSize" Value="{DynamicResource SubHeadFontSize}" />        <Setter Property="Template">            <Setter.Value>                <ControlTemplate TargetType="{x:Type editor:PropertyGridEditorNumberInput}">                    <local:SimplePanel>                        <Border x:Name="root"                                 CornerRadius="{Binding Path=(attach:BorderElement.CornerRadius),RelativeSource={RelativeSource TemplatedParent}}"                                Margin="{TemplateBinding Margin}"                                BorderBrush="{TemplateBinding BorderBrush}"                                 BorderThickness="{TemplateBinding BorderThickness}"                                Background="{TemplateBinding Background}"                                VerticalAlignment="Stretch"                                HorizontalAlignment="Stretch">                            <Grid>                                <Grid.ColumnDefinitions>                                    <ColumnDefinition Width="*" />                                    <ColumnDefinition Width="Auto" />                                    <ColumnDefinition Width="Auto" />                                </Grid.ColumnDefinitions>                                <local:TextBoxNoKeyboard x:Name="PART_TextBox" Grid.Column="0"                                                         BorderThickness="0" Height="Auto"                                                         Background="Transparent"                                                         FontSize="{TemplateBinding FontSize}"                                                         IsReadOnly="{TemplateBinding IsReadOnly}"                                                         InputMethod.IsInputMethodEnabled="False"                                                         TextWrapping="NoWrap"                                                         Foreground="{TemplateBinding Foreground}"                                                         HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"                                                          VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"                                                          Padding="{TemplateBinding Padding}"/>                                <Button Grid.Column="1" Height="Auto"                                         Style="{StaticResource NumberInputButtonStyle}"                                        Background="{TemplateBinding Background}"                                        Foreground="{TemplateBinding Foreground}"                                        FontSize="{TemplateBinding FontSize}"                                        Command="{x:Static local:ControlCommands.Prev}"                                        IsEnabled="{TemplateBinding IsReadOnly, Converter={StaticResource Boolean2BooleanReConverter}}">                                    <TextBlock FontFamily="{DynamicResource IconFont}"                                               Text="&#xe9d6;"                                                FontSize="{TemplateBinding FontSize}"                                                VerticalAlignment="Center"                                                HorizontalAlignment="Center" />                                </Button>                                <Button Grid.Column="2" Height="Auto"                                         Style="{StaticResource NumberInputButtonStyle}"                                        Background="{TemplateBinding Background}"                                        Foreground="{TemplateBinding Foreground}"                                        FontSize="{TemplateBinding FontSize}"                                        Command="{x:Static local:ControlCommands.Next}"                                        IsEnabled="{TemplateBinding IsReadOnly, Converter={StaticResource Boolean2BooleanReConverter}}">                                    <TextBlock FontFamily="{DynamicResource IconFont}"                                                Text="&#xe9d7;"                                                 FontSize="{TemplateBinding FontSize}"                                                 VerticalAlignment="Center"                                                 HorizontalAlignment="Center" />                                </Button>                            </Grid>                        </Border>                    </local:SimplePanel>                    <ControlTemplate.Triggers>                        <Trigger Property="IsEnabled" Value="false">                            <Setter Property="Opacity" TargetName="root" Value="0.4" />                        </Trigger>                        <Trigger Property="IsMouseOver" Value="true">                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />                            <Setter Property="BorderThickness" Value="0,0,0,1" />                        </Trigger>                        <Trigger Property="IsFocused" Value="true" SourceName="PART_TextBox">                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />                            <Setter Property="BorderThickness" Value="0,0,0,1" />                        </Trigger>                    </ControlTemplate.Triggers>                </ControlTemplate>            </Setter.Value>        </Setter>    </Style>    <!-- PropertyGrid ComboBox Editors -->    <Style TargetType="{x:Type editor:PropertyGridEditorComboBox}" BasedOn="{StaticResource InputElementBaseStyle}">        <Setter Property="AutomationProperties.AutomationId" Value="PropertyItemEditor" />        <Setter Property="AutomationProperties.Name" Value="PropertyGridEditorComboBox" />        <Setter Property="attach:BorderElement.CornerRadius" Value="{StaticResource DefaultCornerRadius}" />        <Setter Property="Background" Value="Transparent"/>        <Setter Property="BorderBrush" Value="Transparent"/>        <Setter Property="BorderThickness" Value="0"/>        <Setter Property="ItemContainerStyle">            <Setter.Value>                <Style TargetType="ComboBoxItem">                    <Setter Property="SnapsToDevicePixels" Value="True" />                    <Setter Property="Padding" Value="{StaticResource DefaultControlPadding}" />                    <Setter Property="Background" Value="Transparent" />                    <Setter Property="BorderBrush" Value="Transparent" />                    <Setter Property="BorderThickness" Value="0" />                    <Setter Property="MinHeight" Value="{StaticResource DefaultPropertyItemHeight}" />                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />                    <Setter Property="Template">                        <Setter.Value>                            <ControlTemplate TargetType="ComboBoxItem">                                <Border x:Name="Bd" CornerRadius="{Binding Path=(attach:BorderElement.CornerRadius),RelativeSource={RelativeSource TemplatedParent}}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />                                </Border>                                <ControlTemplate.Triggers>                                    <MultiTrigger>                                        <MultiTrigger.Conditions>                                            <Condition Property="IsSelected" Value="False" />                                            <Condition Property="IsMouseOver" Value="True" />                                        </MultiTrigger.Conditions>                                        <Setter Property="Background" TargetName="Bd" Value="{DynamicResource ThirdlyRegionBrush}" />                                    </MultiTrigger>                                    <MultiTrigger>                                        <MultiTrigger.Conditions>                                            <Condition Property="IsSelected" Value="True" />                                        </MultiTrigger.Conditions>                                        <Setter Property="Background" TargetName="Bd" Value="{DynamicResource PrimaryBrush}" />                                        <Setter Property="Foreground" Value="{DynamicResource TextIconBrush}" />                                    </MultiTrigger>                                </ControlTemplate.Triggers>                            </ControlTemplate>                        </Setter.Value>                    </Setter>                </Style>            </Setter.Value>        </Setter>        <Setter Property="Template">            <Setter.Value>                <ControlTemplate TargetType="ComboBox">                    <Grid>                        <Grid.ColumnDefinitions>                            <ColumnDefinition />                            <ColumnDefinition Width="Auto" />                        </Grid.ColumnDefinitions>                        <Border x:Name="border" Grid.ColumnSpan="2"                                 CornerRadius="{Binding Path=(attach:BorderElement.CornerRadius),RelativeSource={RelativeSource TemplatedParent}}"                                 BorderBrush="{TemplateBinding BorderBrush}"                                 BorderThickness="{TemplateBinding BorderThickness}"                                 Background="{TemplateBinding Background}" />                        <local:ToggleBlock Grid.Column="1" Padding="1,0"                                            Background="Transparent"                                            VerticalContentAlignment="Stretch"                                            HorizontalContentAlignment="Stretch"                                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">                            <local:ToggleBlock.UnCheckedContent>                                <Path x:Name="UnCheckedContent" VerticalAlignment="Center" HorizontalAlignment="Right" Width="14" Stretch="Uniform"                                      Data="{StaticResource DownGeometry}"                                        Fill="{DynamicResource PrimaryTextBrush}"                                       Margin="{Binding Padding, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ThicknessSplitConverter}, ConverterParameter='0,0,1,0'}" />                            </local:ToggleBlock.UnCheckedContent>                            <local:ToggleBlock.CheckedContent>                                <Path x:Name="CheckedContent" VerticalAlignment="Center" HorizontalAlignment="Right" Width="14" Stretch="Uniform"                                       Data="{StaticResource UpGeometry}"                                       Fill="{DynamicResource PrimaryTextBrush}"                                       Margin="{Binding Padding, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ThicknessSplitConverter}, ConverterParameter='0,0,1,0'}" />                            </local:ToggleBlock.CheckedContent>                        </local:ToggleBlock>                        <local:ToggleBlock Grid.Column="0" Grid.ColumnSpan="2"                                            Background="Transparent" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" ToggleGesture="LeftClick"                                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />                        <ContentPresenter Grid.Column="0" Margin="{TemplateBinding Padding}" x:Name="contentPresenter" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Content="{TemplateBinding SelectionBoxItem}" ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="false" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />                        <Popup x:Name="PART_Popup" PlacementTarget="{Binding ElementName=border}" AllowsTransparency="true" IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="1" PopupAnimation="{StaticResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}" Placement="Bottom">                            <Decorator Margin="8 0">                                <Border x:Name="dropDownBorder"                                         BorderThickness="0,1,0,0"                                         Effect="{StaticResource EffectShadow2}" Margin="0,0,0,8"                                         CornerRadius="{Binding Path=(attach:BorderElement.CornerRadius),RelativeSource={RelativeSource TemplatedParent}}"                                        MinWidth="{Binding ActualWidth, ElementName=border}"                                         MaxHeight="{TemplateBinding MaxDropDownHeight}"                                         BorderBrush="{DynamicResource BorderBrush}"                                         Background="{DynamicResource SecondaryRegionBrush}">                                    <local:ToggleBlock IsChecked="{Binding HasItems,RelativeSource={RelativeSource TemplatedParent},Mode=OneWay}" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch">                                        <local:ToggleBlock.CheckedContent>                                            <ScrollViewer Margin="2">                                                <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />                                            </ScrollViewer>                                        </local:ToggleBlock.CheckedContent>                                        <local:ToggleBlock.UnCheckedContent>                                            <local:EmptyContentControl />                                        </local:ToggleBlock.UnCheckedContent>                                    </local:ToggleBlock>                                </Border>                            </Decorator>                        </Popup>                    </Grid>                    <ControlTemplate.Triggers>                        <Trigger Property="HasItems" Value="false">                            <Setter Property="Height" TargetName="dropDownBorder" Value="95" />                        </Trigger>                        <Trigger Property="attach:DropDownElement.ConsistentWidth" Value="True">                            <Setter Property="MaxWidth" TargetName="dropDownBorder" Value="{Binding ActualWidth, ElementName=border}" />                            <Setter Property="MinWidth" TargetName="dropDownBorder" Value="{Binding ActualWidth, ElementName=border}" />                        </Trigger>                        <Trigger Property="attach:DropDownElement.AutoWidth" Value="True">                            <Setter Property="MaxWidth" TargetName="dropDownBorder" Value="{x:Static sys:Double.MaxValue}" />                            <Setter Property="MinWidth" TargetName="dropDownBorder" Value="{Binding ActualHeight, ElementName=border}" />                        </Trigger>                        <MultiTrigger>                            <MultiTrigger.Conditions>                                <Condition Property="IsGrouping" Value="true" />                                <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false" />                            </MultiTrigger.Conditions>                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />                        </MultiTrigger>                        <MultiTrigger>                            <MultiTrigger.Conditions>                                <Condition Property="IsMouseOver" Value="true" />                                <Condition Property="IsOpen" Value="false" SourceName="PART_Popup" />                            </MultiTrigger.Conditions>                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />                            <Setter Property="BorderThickness" Value="0,0,0,1" />                            <Setter TargetName="CheckedContent" Property="Fill" Value="{DynamicResource PrimaryBrush}" />                            <Setter TargetName="UnCheckedContent" Property="Fill" Value="{DynamicResource PrimaryBrush}" />                        </MultiTrigger>                        <Trigger Property="IsOpen" Value="True" SourceName="PART_Popup">                            <Setter Property="ComboBox.BorderBrush" Value="{DynamicResource PrimaryBrush}" />                            <Setter TargetName="CheckedContent" Property="Fill" Value="{DynamicResource PrimaryBrush}" />                            <Setter TargetName="UnCheckedContent" Property="Fill" Value="{DynamicResource PrimaryBrush}" />                        </Trigger>                        <Trigger Property="IsFocused" Value="True">                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />                            <Setter Property="BorderThickness" Value="0,0,0,1" />                            <Setter TargetName="CheckedContent" Property="Fill" Value="{DynamicResource PrimaryBrush}" />                            <Setter TargetName="UnCheckedContent" Property="Fill" Value="{DynamicResource PrimaryBrush}" />                        </Trigger>                        <Trigger Property="IsEnabled" Value="false">                            <Setter Property="Opacity" Value="0.4" />                        </Trigger>                    </ControlTemplate.Triggers>                </ControlTemplate>            </Setter.Value>        </Setter>        <Style.Triggers>            <MultiTrigger>                <MultiTrigger.Conditions>                    <Condition Property="IsKeyboardFocusWithin" Value="False" />                    <Condition Property="IsMouseOver" Value="False" />                </MultiTrigger.Conditions>                <Setter Property="BorderBrush" Value="Transparent" />                <Setter Property="Background" Value="Transparent" />            </MultiTrigger>            <Trigger Property="IsReadOnly" Value="True">                <Setter Property="IsEnabled" Value="False" />            </Trigger>            <Trigger Property="IsEnabled" Value="False">                <Setter Property="Foreground" Value="{DynamicResource DarkMaskBrush }" />            </Trigger>        </Style.Triggers>    </Style>    <!-- PropertyGrid Enum CheckBox Editors -->    <Style TargetType="{x:Type editor:PropertyGridEditorEnumCheckComboBox}" BasedOn="{StaticResource InputElementBaseStyle}">        <Setter Property="AutomationProperties.AutomationId" Value="PropertyItemEditor" />        <Setter Property="AutomationProperties.Name" Value="PropertyGridEditorComboBox" />        <Setter Property="attach:BorderElement.CornerRadius" Value="{StaticResource DefaultCornerRadius}" />        <Setter Property="Background" Value="Transparent"/>        <Setter Property="BorderBrush" Value="Transparent"/>        <Setter Property="BorderThickness" Value="0,0,0,1"/>        <Setter Property="Padding" Value="0,0" />        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />        <Setter Property="ScrollViewer.PanningMode" Value="Both" />        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />        <Setter Property="VerticalContentAlignment" Value="Center" />        <Setter Property="HorizontalContentAlignment" Value="Stretch" />        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />        <Setter Property="Template">            <Setter.Value>                <ControlTemplate TargetType="{x:Type local:CheckComboBox}">                    <Grid x:Name="MainGrid" SnapsToDevicePixels="true">                        <Popup x:Name="PART_Popup"                               AllowsTransparency="true"                               IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"                               StaysOpen="False"                               Margin="1"                               PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}"                               Placement="Bottom">                            <Grid MinWidth="{Binding ActualWidth, ElementName=MainGrid}">                                <Border x:Name="DropDownBorder"                                        MaxHeight="{Binding MaxDropDownHeight,  RelativeSource={RelativeSource TemplatedParent}}"                                        BorderBrush="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=BorderBrush}"                                        Background="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Background}"                                        BorderThickness="1">                                    <ScrollViewer x:Name="DropDownScrollViewer">                                        <Grid RenderOptions.ClearTypeHint="Enabled">                                            <Canvas HorizontalAlignment="Left" Height="0" VerticalAlignment="Top" Width="0">                                                <Rectangle x:Name="OpaqueRect"                                                           Fill="{Binding Background, ElementName=DropDownBorder}"                                                           Height="{Binding ActualHeight, ElementName=DropDownBorder}"                                                           Width="{Binding ActualWidth, ElementName=DropDownBorder}" />                                            </Canvas>                                            <StackPanel>                                                <prim:SelectAllSelectorItem x:Name="PART_SelectAllSelectorItem"                                                                            Content="{TemplateBinding SelectAllContent}"                                                                            Visibility="{Binding IsSelectAllActive, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityConverter}}" />                                                <ItemsPresenter x:Name="PART_ItemsPresenter"                                                                KeyboardNavigation.DirectionalNavigation="Contained"                                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />                                            </StackPanel>                                        </Grid>                                    </ScrollViewer>                                </Border>                            </Grid>                        </Popup>                        <Border Background="{TemplateBinding Background}"                                 BorderBrush="{TemplateBinding BorderBrush}"                                 BorderThickness="{TemplateBinding BorderThickness}" />                        <ToggleButton x:Name="PART_DropDownButton"                                      Content="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"                                      BorderBrush="{TemplateBinding BorderBrush}"                                      Background="{TemplateBinding Background}"                                      Focusable="False"                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"                                      Style="{StaticResource ComboBoxToggleButton}"                                      Padding="{TemplateBinding Padding}"                                      VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"                                      HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"                                      IsHitTestVisible="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2BooleanReConverter}}" >                        </ToggleButton>                        <ContentPresenter x:Name="WatermarkHost"                                          Content="{TemplateBinding Watermark}"                                          ContentTemplate="{Binding WatermarkTemplate, RelativeSource={RelativeSource TemplatedParent}, TargetNullValue={StaticResource DefaultWatermarkTemplate}}"                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"                                          IsHitTestVisible="False"                                          Margin="{TemplateBinding Padding}"                                          Visibility="Collapsed" />                    </Grid>                    <ControlTemplate.Triggers>                        <Trigger Property="Text" Value="" >                            <Setter Property="Visibility" Value="Visible" TargetName="WatermarkHost"/>                        </Trigger>                    </ControlTemplate.Triggers>                </ControlTemplate>            </Setter.Value>        </Setter>        <Style.Triggers>            <Trigger Property="IsEditable" Value="True">                <Setter Property="IsTabStop" Value="False" />            </Trigger>        </Style.Triggers>    </Style>    <!-- PropertyGrid CheckBox Editors -->    <Style TargetType="{x:Type editor:PropertyGridEditorCheckBox}" BasedOn="{StaticResource {x:Type CheckBox}}">        <Setter Property="Margin" Value="5,0,0,0" />        <Setter Property="Margin" Value="5,0,0,0" />    </Style>    <!-- PropertyGrid TextBox Editors -->    <Style TargetType="{x:Type editor:PropertyGridEditorTextBox}" BasedOn="{StaticResource {x:Type local:WatermarkTextBox} }">        <Style.Triggers>            <MultiTrigger>                <MultiTrigger.Conditions>                    <Condition Property="IsKeyboardFocusWithin" Value="False" />                    <Condition Property="IsMouseOver" Value="False" />                </MultiTrigger.Conditions>                <Setter Property="BorderBrush" Value="Transparent" />            </MultiTrigger>            <MultiTrigger>                <MultiTrigger.Conditions>                    <Condition Property="IsEnabled" Value="False" />                </MultiTrigger.Conditions>                <Setter Property="BorderThickness" Value="0" />            </MultiTrigger>        </Style.Triggers>    </Style>    <!-- PropertyGrid TextBlock Editors -->    <Style TargetType="{x:Type editor:PropertyGridEditorTextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">        <Setter Property="HorizontalAlignment" Value="Left" />        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />        <Setter Property="Margin" Value="5,0,0,0" />        <Setter Property="TextTrimming" Value="CharacterEllipsis" />    </Style>    <!-- PropertyGrid Password Editors -->    <Style x:Key="{x:Type editor:PropertyGridEditorWatermarkPasswordBox}" TargetType="{x:Type editor:PropertyGridEditorWatermarkPasswordBox}" BasedOn="{StaticResource {x:Type local:WatermarkTextBox}}">        <Setter Property="BorderThickness" Value="0" />        <Setter Property="AutomationProperties.AutomationId" Value="PropertyItemEditor" />        <Setter Property="AutomationProperties.Name" Value="PropertyGridEditorWatermarkPasswordBox" />    </Style>    <!-- PropertyGrid FilePicker Editors -->    <Style TargetType="{x:Type editor:PropertyGridEditorFilePicker}" BasedOn="{StaticResource {x:Type local:FilePicker}}">        <Setter Property="AutomationProperties.AutomationId" Value="PropertyItemEditor" />        <Setter Property="Background" Value="Transparent" />        <Style.Triggers>            <MultiTrigger>                <MultiTrigger.Conditions>                    <Condition Property="IsKeyboardFocusWithin" Value="False" />                    <Condition Property="IsMouseOver" Value="False" />                </MultiTrigger.Conditions>                <Setter Property="BorderBrush" Value="Transparent" />            </MultiTrigger>        </Style.Triggers>    </Style>    <!-- PropertyGrid CollectionControl Editors -->    <Style TargetType="{x:Type editor:PropertyGridEditorCollectionControl}" BasedOn="{StaticResource CollectionControlButton}">        <Setter Property="AutomationProperties.AutomationId" Value="PropertyItemEditor" />        <Setter Property="Background" Value="Transparent"/>    </Style>    <!-- PropertyGrid PrimitiveTypeCollectionControl Editors -->    <Style TargetType="{x:Type editor:PropertyGridEditorPrimitiveTypeCollectionControl}" BasedOn="{StaticResource PrimitiveTypeCollectionControl}">        <Setter Property="AutomationProperties.AutomationId" Value="PropertyItemEditor" />        <Setter Property="Content" Value="(Collection)" />        <Setter Property="BorderThickness" Value="0" />        <Setter Property="BorderThickness" Value="0" />        <Setter Property="Content" Value="(Collection)" />    </Style>    <!-- PropertyGrid DateTimeUpDown Editors -->    <Style TargetType="{x:Type editor:PropertyGridEditorDateTimeUpDown}" BasedOn="{StaticResource DateTimeUpDownBaseStyle}">        <Setter Property="AutomationProperties.AutomationId" Value="PropertyItemEditor" />        <Setter Property="TextAlignment" Value="Left" />        <Setter Property="Background" Value="Transparent" />        <Style.Triggers>            <MultiTrigger>                <MultiTrigger.Conditions>                    <Condition Property="UIElement.IsKeyboardFocusWithin" Value="False" />                    <Condition Property="UIElement.IsMouseOver" Value="False" />                </MultiTrigger.Conditions>                <Setter Property="BorderBrush" Value="#00FFFFFF" />                <Setter Property="ShowButtonSpinner" Value="False" />            </MultiTrigger>        </Style.Triggers>    </Style>    <!-- PropertyGrid Position Editors -->    <Style TargetType="{x:Type editor:PropertyGridEditorPosition}" BasedOn="{StaticResource BaseStyle }">        <Setter Property="AutomationProperties.AutomationId" Value="PropertyGridEditorPosition" />        <Setter Property="Background" Value="Transparent" />        <Setter Property="HorizontalContentAlignment" Value="Left" />        <Setter Property="VerticalContentAlignment" Value="Center" />        <Setter Property="Margin" Value="10,0" />        <Setter Property="Template">            <Setter.Value>                <ControlTemplate TargetType="{x:Type editor:PropertyGridEditorPosition}">                    <Grid>                        <Grid.ColumnDefinitions>                            <ColumnDefinition Width="*" />                            <ColumnDefinition Width="Auto" />                            <ColumnDefinition Width="Auto" />                        </Grid.ColumnDefinitions>                        <!-- 坐标值显示/输入 -->                        <ContentPresenter ContentSource="Value"                                           HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"                                          Margin="{TemplateBinding Padding}"/>                        <!-- 拾取按钮 -->                        <Button Grid.Column="1"                                 Content="{TemplateBinding PickUpLabel}"                                 Height="{DynamicResource DefaultPropertyItemHeight}"                                Margin="10,0,0,0"                                 Padding="15,1"                                Visibility="{TemplateBinding IsPickUpVisible, Converter={StaticResource Boolean2VisibilityConverter}}"                                Command="{TemplateBinding PickUpCommand}"                                CommandParameter="{Binding PropertyItemInstance}"/>                        <!-- 定位按钮 -->                        <Button Grid.Column="2"                                 Content="{TemplateBinding LocateLabel}"                                Height="{DynamicResource DefaultPropertyItemHeight}"                                Margin="10,0,0,0"                                 Padding="15,1"                                Visibility="{TemplateBinding IsLocateVisible, Converter={StaticResource Boolean2VisibilityConverter}}"                                Command="{TemplateBinding LocateCommand}"                                CommandParameter="{TemplateBinding Value}"/>                    </Grid>                </ControlTemplate>            </Setter.Value>        </Setter>        <Style.Triggers>            <MultiTrigger>                <MultiTrigger.Conditions>                    <Condition Property="UIElement.IsKeyboardFocusWithin" Value="False" />                    <Condition Property="UIElement.IsMouseOver" Value="False" />                </MultiTrigger.Conditions>                <Setter Property="BorderBrush" Value="#00FFFFFF" />            </MultiTrigger>        </Style.Triggers>    </Style>    <!--PropertyGrid Command Editor-->    <Style TargetType="{x:Type editor:PropertyGridEditorCommand}" BasedOn="{StaticResource ButtonBaseStyle}">        <Setter Property="HorizontalAlignment" Value="Left" />        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>        <Setter Property="Width" Value="120" />        <Setter Property="Height" Value="{DynamicResource DefaultPropertyItemHeight}" />        <Setter Property="Margin" Value="5,0,0,0" />    </Style>    <Style x:Key="OptionsToggleButtonStyle" TargetType="{x:Type RadioButton}">        <Setter Property="Background" Value="#00FFFFFF" />        <Setter Property="BorderBrush" Value="#00FFFFFF" />        <Setter Property="BorderThickness" Value="1" />        <Setter Property="FocusVisualStyle" Value="{x:Null}" />        <Setter Property="Height" Value="22" />        <Setter Property="HorizontalContentAlignment" Value="Center" />        <Setter Property="ToolTipService.InitialShowDelay" Value="900" />        <Setter Property="ToolTipService.ShowDuration" Value="20000" />        <Setter Property="ToolTipService.BetweenShowDelay" Value="0" />        <Setter Property="Template">            <Setter.Value>                <ControlTemplate TargetType="{x:Type RadioButton}">                    <Grid SnapsToDevicePixels="True">                        <Border x:Name="OuterBorder"                                BorderBrush="{TemplateBinding BorderBrush}"                                BorderThickness="{TemplateBinding BorderThickness}"                                Background="{TemplateBinding Background}"                                CornerRadius="2" />                        <Border x:Name="MiddleBorder"                                 BorderBrush="#00FFFFFF"                                 BorderThickness="{TemplateBinding BorderThickness}"                                 Background="#00FFFFFF"                                 CornerRadius="2">                            <Border x:Name="InnerBorder"                                     BorderBrush="#00FFFFFF"                                     BorderThickness="{TemplateBinding BorderThickness}"                                     Background="#00FFFFFF"                                     Padding="{TemplateBinding Padding}"                                    CornerRadius="2">                                <StackPanel x:Name="StackPanel" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">                                    <ContentPresenter Name="Content"                                                       Content="{TemplateBinding Content}" Margin="1"                                                       RenderOptions.BitmapScalingMode="NearestNeighbor"                                                       VerticalAlignment="Center"                                                       HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />                                </StackPanel>                            </Border>                        </Border>                    </Grid>                    <ControlTemplate.Triggers>                        <Trigger Property="IsMouseOver" Value="True">                            <Setter TargetName="OuterBorder" Property="Background" Value="{StaticResource MouseOverBackgroundBrush}" />                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource MouseOverBorderBrush}" />                            <Setter Property="BorderBrush" TargetName="InnerBorder" Value="#80FFFFFF" />                        </Trigger>                        <Trigger Property="IsEnabled" Value="False">                            <Setter TargetName="Content" Property="Opacity" Value="0.5" />                            <Setter TargetName="OuterBorder" Property="TextElement.Foreground" Value="#FF9E9E9E" />                        </Trigger>                        <Trigger Property="IsChecked" Value="True">                            <Setter TargetName="OuterBorder" Property="Background" Value="{StaticResource CheckedBackgroundBrush}" />                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource CheckedBorderBrush}" />                            <Setter TargetName="InnerBorder" Property="BorderBrush">                                <Setter.Value>                                    <LinearGradientBrush EndPoint="0,1" StartPoint="0,0">                                        <GradientStop Color="#FFE7CBAD" Offset="0" />                                        <GradientStop Color="#FFF7D7B5" Offset="0.1" />                                        <GradientStop Color="#FFFFD38C" Offset="0.36" />                                        <GradientStop Color="#FFFFC75A" Offset="0.36" />                                        <GradientStop Color="#FFFFEFA5" Offset="1" />                                    </LinearGradientBrush>                                </Setter.Value>                            </Setter>                        </Trigger>                        <Trigger Property="IsPressed" Value="True">                            <Setter TargetName="OuterBorder" Property="Background" Value="{StaticResource PressedBackgroundBrush}" />                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource PressedBorderBrush}" />                            <Setter TargetName="InnerBorder" Property="Border.BorderBrush" Value="#00FFFFFF" />                        </Trigger>                        <MultiTrigger>                            <MultiTrigger.Conditions>                                <Condition Property="IsChecked"  Value="True" />                                <Condition Property="IsMouseOver"  Value="True" />                            </MultiTrigger.Conditions>                            <Setter TargetName="MiddleBorder" Property="Background">                                <Setter.Value>                                    <LinearGradientBrush EndPoint="0,1" StartPoint="0,0">                                        <GradientStop Color="#40FFFEFE" Offset="0" />                                        <GradientStop Color="#40FFFEFE" Offset="0.39" />                                        <GradientStop Color="#20FFCE68" Offset="0.39" />                                        <GradientStop Color="#20FFCE68" Offset="0.69" />                                        <GradientStop Color="#10FFFFFF" Offset="1" />                                    </LinearGradientBrush>                                </Setter.Value>                            </Setter>                        </MultiTrigger>                    </ControlTemplate.Triggers>                </ControlTemplate>            </Setter.Value>        </Setter>    </Style>    <Style x:Key="ClearFilterButtonStyle" TargetType="{x:Type Button}">        <Setter Property="Background" Value="Transparent" />        <Setter Property="BorderBrush" Value="Transparent" />        <Setter Property="BorderThickness" Value="1" />        <Setter Property="Height" Value="22" />        <Setter Property="HorizontalContentAlignment" Value="Center" />        <Setter Property="ToolTipService.InitialShowDelay" Value="900" />        <Setter Property="ToolTipService.ShowDuration" Value="20000" />        <Setter Property="ToolTipService.BetweenShowDelay" Value="0" />        <Setter Property="Template">            <Setter.Value>                <ControlTemplate TargetType="{x:Type Button}">                    <Grid SnapsToDevicePixels="True">                        <Border x:Name="OuterBorder"                                BorderBrush="{TemplateBinding BorderBrush}"                                BorderThickness="{TemplateBinding BorderThickness}"                                Background="{TemplateBinding Background}"                                CornerRadius="2" />                        <Border x:Name="MiddleBorder"                                BorderBrush="Transparent"                                BorderThickness="{TemplateBinding BorderThickness}"                                Background="Transparent"                                CornerRadius="2">                            <Border x:Name="InnerBorder"                                    BorderBrush="Transparent"                                    BorderThickness="{TemplateBinding BorderThickness}"                                    Background="Transparent"                                    CornerRadius="2"                                    Padding="{TemplateBinding Padding}">                                <StackPanel x:Name="StackPanel" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">                                    <ContentPresenter x:Name="Content"                                                      Content="{TemplateBinding Content}"                                                      Margin="1"                                                      RenderOptions.BitmapScalingMode="NearestNeighbor"                                                      VerticalAlignment="Center"                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />                                </StackPanel>                            </Border>                        </Border>                    </Grid>                    <ControlTemplate.Triggers>                        <Trigger Property="IsMouseOver" Value="True">                            <Setter TargetName="OuterBorder" Property="Background"  Value="{StaticResource MouseOverBackgroundBrush}" />                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource MouseOverBorderBrush}" />                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="#80FFFFFF" />                        </Trigger>                        <Trigger Property="IsEnabled" Value="False">                            <Setter TargetName="Content" Property="Opacity" Value="0.5" />                            <Setter TargetName="OuterBorder" Property="TextElement.Foreground" Value="#FF9E9E9E" />                        </Trigger>                        <Trigger Property="IsPressed" Value="True">                            <Setter Property="Background" TargetName="OuterBorder" Value="{StaticResource PressedBackgroundBrush}" />                            <Setter Property="BorderBrush" TargetName="OuterBorder" Value="{StaticResource PressedBorderBrush}" />                            <Setter Property="BorderBrush" TargetName="InnerBorder" Value="Transparent" />                        </Trigger>                    </ControlTemplate.Triggers>                </ControlTemplate>            </Setter.Value>        </Setter>    </Style>    <DataTemplate x:Key="DefaultCategoryGroupHeaderTemplate">        <TextBlock Text="{Binding}" HorizontalAlignment="Left"/>    </DataTemplate>    <ControlTemplate x:Key="ExpanderToggleButton" TargetType="{x:Type ToggleButton}">        <Grid>            <Grid.ColumnDefinitions>                <ColumnDefinition Width="20" />                <ColumnDefinition Width="*" />            </Grid.ColumnDefinitions>            <Rectangle Margin="0,0,0,0" x:Name="Rectangle" Fill="Transparent" />            <Path x:Name="Up_Arrow"                   HorizontalAlignment="Center"                   VerticalAlignment="Center"                  Fill="{DynamicResource GeometryBrush}"                  Data="M 0 0 L 4 4 L 8 0 Z"                  RenderTransformOrigin="0.5,0.5">                <Path.RenderTransform>                    <TransformGroup>                        <ScaleTransform ScaleX="1" ScaleY="1" />                        <SkewTransform AngleX="0" AngleY="0" />                        <RotateTransform Angle="-90" />                        <TranslateTransform X="0" Y="0" />                    </TransformGroup>                </Path.RenderTransform>            </Path>            <Path x:Name="Down_Arrow"                  Visibility="Collapsed"                  HorizontalAlignment="Center"                                    VerticalAlignment="Center"                  Fill="{DynamicResource GeometryBrush}"                  Data="M 0 4 L 4 0 L 8 4 Z"                  RenderTransformOrigin="0.5,0.5">                <Path.RenderTransform>                    <TransformGroup>                        <ScaleTransform ScaleX="1" ScaleY="1" />                        <SkewTransform AngleX="0" AngleY="0" />                        <RotateTransform Angle="135" />                        <TranslateTransform X="0" Y="0" />                    </TransformGroup>                </Path.RenderTransform>            </Path>            <ContentPresenter Grid.Column="1" />        </Grid>        <ControlTemplate.Triggers>            <Trigger Property="IsChecked" Value="true">                <Setter TargetName="Down_Arrow" Property="Visibility" Value="Visible" />                <Setter TargetName="Up_Arrow" Property="Visibility" Value="Collapsed" />                <Setter TargetName="Down_Arrow" Property="OpacityMask"  Value="#FF000000" />            </Trigger>        </ControlTemplate.Triggers>    </ControlTemplate>    <Style x:Key="ExpanderStyle" TargetType="{x:Type Expander}">        <Setter Property="Padding" Value="0" />        <Setter Property="Background" Value="{DynamicResource RegionBrush}" />        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />        <Setter Property="attach:BorderElement.CornerRadius" Value="{StaticResource DefaultCornerRadius}" />        <Setter Property="Template">            <Setter.Value>                <ControlTemplate TargetType="{x:Type Expander}">                    <Grid>                        <Grid.RowDefinitions>                            <RowDefinition Height="Auto" />                            <RowDefinition x:Name="ContentRow" Height="*"/>                        </Grid.RowDefinitions>                        <Border x:Name="Border"                                 Background="{TemplateBinding Background}"                                 BorderBrush="{TemplateBinding BorderBrush}"                                BorderThickness="{TemplateBinding BorderThickness}">                            <ToggleButton AutomationProperties.AutomationId="ExpanderToggleButton"                                           AutomationProperties.Name="ExpanderToggleButton"                                          Template="{StaticResource ExpanderToggleButton}"                                          HorizontalAlignment="Stretch"                                          VerticalAlignment="Stretch"                                          IsChecked="{Binding Path=IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"                                          OverridesDefaultStyle="True">                                <ContentPresenter Margin="1" RecognizesAccessKey="True"                                                   ContentSource="Header"                                                   TextElement.Foreground="{TemplateBinding Foreground}"                                                  TextElement.FontSize="{TemplateBinding FontSize}"                                                  TextElement.FontWeight="Bold" />                            </ToggleButton>                        </Border>                        <Border x:Name="ExpandSite" Grid.Row="1" Visibility="Collapsed"                                 Background="{TemplateBinding Background}"                                Padding="10 0 0 0">                            <Border BorderThickness="0" Margin="0" Padding="0"                                     VerticalAlignment="Stretch"                                     HorizontalAlignment="Stretch">                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"                                                  Margin="{TemplateBinding Padding}"                                                  Focusable="False" />                            </Border>                        </Border>                    </Grid>                    <ControlTemplate.Triggers>                        <Trigger Property="IsExpanded" Value="True">                            <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible" />                        </Trigger>                    </ControlTemplate.Triggers>                </ControlTemplate>            </Setter.Value>        </Setter>    </Style>    <Style x:Key="PropertyExpanderStyle" TargetType="{x:Type Expander}">        <Setter Property="Padding" Value="10 0 0 0" />        <Setter Property="Template">            <Setter.Value>                <ControlTemplate TargetType="{x:Type Expander}">                    <Grid>                        <Border x:Name="ExpandSite" Visibility="Collapsed" Grid.Row="1" Padding="0 0 0 0">                            <Border BorderThickness="0" Margin="0" Padding="0" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch">                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"                                                  Focusable="False" />                            </Border>                        </Border>                    </Grid>                    <ControlTemplate.Triggers>                        <Trigger Property="IsExpanded" Value="True">                            <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible" />                        </Trigger>                    </ControlTemplate.Triggers>                </ControlTemplate>            </Setter.Value>        </Setter>    </Style>    <Style x:Key="PropertyItemGroupContainerStyle" TargetType="{x:Type GroupItem}">        <Style.Resources>            <ResourceDictionary>                <pgconv:IsCustomPropertyItemConverter x:Key="IsCustomPropertyItemConverter" />                <pgconv:CategoryExpanderConverter x:Key="CategoryExpanderConverter" />            </ResourceDictionary>        </Style.Resources>        <Setter Property="Template">            <Setter.Value>                <ControlTemplate>                    <Border>                        <Expander x:Name="expander"                                    Style="{StaticResource ExpanderStyle}"                                  AutomationProperties.AutomationId="GroupExpander"                                  IsExpanded="True"                                  Header="{Binding Name}"                                  HeaderTemplate="{Binding CategoryGroupHeaderTemplate, RelativeSource={RelativeSource AncestorType={x:Type pg:PropertyGrid}}}">                            <ItemsPresenter/>                        </Expander>                    </Border>                    <ControlTemplate.Triggers>                        <DataTrigger Binding="{Binding Items.CurrentItem, Converter={StaticResource IsCustomPropertyItemConverter}}" Value="True">                            <Setter TargetName="expander" Property="IsExpanded">                                <Setter.Value>                                    <MultiBinding Converter="{StaticResource CategoryExpanderConverter}">                                        <Binding Path="Items.CurrentItem.IsCategoryExpanded" />                                        <Binding Path="Items.CurrentItem" Mode="OneWay" />                                        <Binding Path="Items" Mode="OneWay" />                                    </MultiBinding>                                </Setter.Value>                            </Setter>                        </DataTrigger>                    </ControlTemplate.Triggers>                </ControlTemplate>            </Setter.Value>        </Setter>        <Style.Triggers>            <MultiDataTrigger>                <MultiDataTrigger.Conditions>                    <Condition Binding="{Binding Name, Converter={StaticResource IsDefaultCategoryConverter}}" Value="True" />                    <Condition Binding="{Binding IsMiscCategoryLabelHidden, RelativeSource={RelativeSource AncestorType={x:Type pg:PropertyGrid}}}"  Value="True" />                </MultiDataTrigger.Conditions>                <Setter Property="Template">                    <Setter.Value>                        <ControlTemplate TargetType="{x:Type GroupItem}">                            <ItemsPresenter />                        </ControlTemplate>                    </Setter.Value>                </Setter>            </MultiDataTrigger>        </Style.Triggers>    </Style>    <Style x:Key="SingleDefaultCategoryItemGroupContainerStyle" TargetType="{x:Type GroupItem}">        <Setter Property="Template">            <Setter.Value>                <ControlTemplate>                    <Border>                        <ItemsPresenter/>                    </Border>                </ControlTemplate>            </Setter.Value>        </Setter>    </Style>    <pg:PropertyGridContextMenu x:Key="SelectedObjectAdvancedOptionsMenu"                                Style="{DynamicResource ContextMenuBaseStyle}"                                attach:MenuAttach.ItemMinHeight="26">        <MenuItem Header="{markup:StringResource 复制值}"                   Command="{x:Static commands:PropertyItemCommands.CopyValue}"                   CommandParameter="{Binding}"                   CommandTarget="{Binding Path=PlacementTarget, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}}}" />        <MenuItem Header="{markup:StringResource 重置}"                   Command="{x:Static commands:PropertyItemCommands.ResetValue}"                   CommandParameter="{Binding}"                   CommandTarget="{Binding Path=PlacementTarget, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContextMenu}}}" />    </pg:PropertyGridContextMenu>    <pg:CategoryGroupStyleSelector x:Key="categoryGroupStyleSelector"                                    SingleDefaultCategoryItemGroupStyle="{StaticResource SingleDefaultCategoryItemGroupContainerStyle}"                                    ItemGroupStyle="{StaticResource PropertyItemGroupContainerStyle}" />    <Style x:Key="PropertyItemBaseStyle" TargetType="{x:Type pg:PropertyItemBase}">        <Setter Property="Background" Value="{DynamicResource SecondaryRegionBrush}" />        <Setter Property="Margin" Value="0,0,0,1" />        <Setter Property="VerticalContentAlignment" Value="Center" />        <Setter Property="SnapsToDevicePixels" Value="True" />        <Setter Property="IsTabStop" Value="False" />        <Setter Property="MinHeight" Value="{StaticResource DefaultPropertyItemHeight}" />        <Setter Property="Template">            <Setter.Value>                <ControlTemplate TargetType="{x:Type pg:PropertyItemBase}">                    <Border ContextMenuService.Placement="Bottom"                             ContextMenu="{Binding AdvancedOptionsMenu, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=pg:PropertyGrid}}">                        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">                            <Grid.ColumnDefinitions>                                <ColumnDefinition Width="{Binding NameColumnWidth, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type pg:PropertyGrid}}}" />                                <ColumnDefinition Width="*" />                            </Grid.ColumnDefinitions>                            <Grid.RowDefinitions>                                <RowDefinition />                                <RowDefinition Height="Auto" />                            </Grid.RowDefinitions>                            <Border Name="PART_Name" Grid.Row="0" Grid.Column="0"                                    Background="{TemplateBinding Background}"                                     BorderBrush="{TemplateBinding BorderBrush}"                                     BorderThickness="{TemplateBinding BorderThickness}"                                    Margin="0,0,1,0"                                     VerticalAlignment="Stretch" HorizontalAlignment="Stretch">                                <Grid Margin="2,2,2,2" VerticalAlignment="{TemplateBinding VerticalContentAlignment}">                                    <Grid.ColumnDefinitions>                                        <ColumnDefinition Width="*" />                                        <ColumnDefinition Width="Auto" />                                    </Grid.ColumnDefinitions>                                    <Grid Margin="{Binding Level, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ExpandableObjectMarginConverter}}">                                        <Grid.ColumnDefinitions>                                            <ColumnDefinition Name="_expandableButtonColumn" Width="{Binding PropertyNameLeftPadding, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type pg:PropertyGrid}}}" />                                            <ColumnDefinition Width="*" />                                        </Grid.ColumnDefinitions>                                        <ToggleButton Name="_expandableButton" Grid.Column="0"                                                       OverridesDefaultStyle="True"                                                       AutomationProperties.AutomationId="ExpandableButton"                                                       AutomationProperties.Name="ExpandableButton"                                                       Template="{StaticResource ExpanderToggleButton}"                                                       IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"                                                       Visibility="{Binding IsExpandable, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityConverter}}" />                                        <pg:TrimmedTextBlock x:Name="_text" Grid.Column="1"                                                              HorizontalAlignment="Stretch"                                                              VerticalAlignment="Center"                                                              AutomationProperties.AutomationId="PropertyItemName"                                                              Text="{Binding DisplayName, RelativeSource={RelativeSource TemplatedParent}}"                                                              HighlightedText="{Binding HighlightedText, RelativeSource={RelativeSource TemplatedParent}}"                                                              TextTrimming="{Binding PropertyNameTextWrapping, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=pg:PropertyGrid}, Converter={StaticResource PropertyNameTextTrimmingConverter}}"                                                              TextWrapping="{Binding PropertyNameTextWrapping, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=pg:PropertyGrid}}"                                                              IsEnabled="{Binding IsReadOnly, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=pg:PropertyGrid}, Converter={StaticResource Boolean2BooleanReConverter}}" />                                    </Grid>                                    <Grid Column="1" HorizontalAlignment="Right" Margin="5,0,5,0"                                           utilities:ContextMenuUtilities.OpenOnMouseLeftButtonClick="True"                                           Visibility="{Binding ShowAdvancedOptions, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=pg:PropertyGrid}, Converter={StaticResource Boolean2VisibilityConverter}}"                                           ContextMenu="{Binding AdvancedOptionsMenu, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=pg:PropertyGrid}}">                                        <Image x:Name="_optionsImage" Width="11" Height="11"                                               AutomationProperties.AutomationId="OptionImage"                                                AutomationProperties.Name="OptionImage"                                                ToolTip="{Binding AdvancedOptionsTooltip, RelativeSource={RelativeSource TemplatedParent}}" />                                    </Grid>                                </Grid>                            </Border>                            <!--显示Editor-->                            <Border Name="PART_Editor" Grid.Row="0" Grid.Column="1"                                    Background="{TemplateBinding Background}"                                    BorderBrush="{TemplateBinding BorderBrush}"                                     BorderThickness="{TemplateBinding BorderThickness}"                                     KeyboardNavigation.DirectionalNavigation="None"                                     VerticalAlignment="Stretch"                                     HorizontalAlignment="Stretch">                                <FrameworkElement.ToolTip>                                    <ToolTip Visibility="Collapsed" />                                </FrameworkElement.ToolTip>                                <ContentControl x:Name="PART_ValueContainer" Focusable="False" IsTabStop="False"                                                 VerticalAlignment="{TemplateBinding VerticalContentAlignment}"                                                HorizontalContentAlignment="Left"                                                VerticalContentAlignment="Center">                                    <ContentControl.Content>                                        <MultiBinding Converter="{StaticResource PropertyItemEditorConverter}">                                            <Binding Path="Editor" RelativeSource="{RelativeSource TemplatedParent}" />                                            <Binding Path="IsReadOnly" RelativeSource="{RelativeSource AncestorType={x:Type pg:PropertyGrid}}" />                                            <Binding Path="IsReadOnly" RelativeSource="{RelativeSource TemplatedParent}" />                                        </MultiBinding>                                    </ContentControl.Content>                                </ContentControl>                            </Border>                            <!--类中类 Editor-->                            <Expander Name="_propertyExpander" Grid.Row="1" Grid.ColumnSpan="2"                                      AutomationProperties.AutomationId="ProtertyItemExpander"                                       AutomationProperties.Name="PropertyItemExpander"                                      Style="{StaticResource PropertyExpanderStyle}"                                      IsExpanded="{TemplateBinding IsExpanded}"                                       IsEnabled="True" >                                <pg:PropertyItemsControl x:Name="PART_PropertyItemsControl"                                                          IsTabStop="False"                                                          Focusable="False"                                                          VirtualizingPanel.VirtualizationMode="Recycling"                                                         VirtualizingPanel.IsVirtualizing="{Binding IsVirtualizing, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=pg:PropertyGrid}}"                                                         ItemsSource="{Binding Properties, RelativeSource={RelativeSource TemplatedParent}}"                                                          ItemContainerStyle="{Binding PropertyContainerStyle,RelativeSource={RelativeSource TemplatedParent}}"                                                         ScrollViewer.CanContentScroll="{Binding IsVirtualizing, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=pg:PropertyGrid}}">                                    <pg:PropertyItemsControl.ItemsPanel>                                        <ItemsPanelTemplate>                                            <VirtualizingStackPanel />                                        </ItemsPanelTemplate>                                    </pg:PropertyItemsControl.ItemsPanel>                                    <pg:PropertyItemsControl.GroupStyle>                                        <GroupStyle ContainerStyleSelector="{StaticResource categoryGroupStyleSelector}" />                                    </pg:PropertyItemsControl.GroupStyle>                                    <pg:PropertyItemsControl.Template>                                        <ControlTemplate>                                            <Border BorderThickness="{TemplateBinding BorderThickness}"                                                     Padding="{TemplateBinding Padding}"                                                     BorderBrush="{TemplateBinding BorderBrush}"                                                     Background="{TemplateBinding Background}"                                                     SnapsToDevicePixels="True">                                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"                                                                 TextBlock.Foreground="{DynamicResource PrimaryTextBrush}"/>                                            </Border>                                        </ControlTemplate>                                    </pg:PropertyItemsControl.Template>                                </pg:PropertyItemsControl>                            </Expander>                        </Grid>                    </Border>                    <ControlTemplate.Triggers>                        <Trigger Property="UIElement.IsMouseOver" Value="True">                            <Setter TargetName="PART_Name" Property="Border.Background" Value="#CED4DF" />                        </Trigger>                        <Trigger Property="pg:PropertyItemBase.IsSelected" Value="True">                            <Setter TargetName="PART_Name" Property="Border.Background" Value="#43577B" />                            <Setter TargetName="PART_Name" Property="TextElement.Foreground" Value="#FFFFFFFF" />                        </Trigger>                        <Trigger Property="IsEnabled" Value="False">                            <Setter TargetName="PART_ValueContainer" Property="Foreground"  Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>                            <Setter TargetName="_text" Property="TextBlock.Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"  />                        </Trigger>                        <MultiDataTrigger>                            <MultiDataTrigger.Conditions>                                <Condition Value="True" Binding="{Binding ShowDescriptionByTooltip, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=pg:PropertyGrid}}" />                                <Condition Value="False" Binding="{Binding Description, RelativeSource={RelativeSource Self}, Converter={StaticResource IsStringEmptyConverter}}" />                            </MultiDataTrigger.Conditions>                            <Setter TargetName="PART_Name" Property="ToolTipService.ToolTip" Value="{Binding Description, RelativeSource={RelativeSource TemplatedParent}}" />                        </MultiDataTrigger>                        <Trigger Property="Validation.HasError" Value="true">                            <Setter Property="FrameworkElement.ToolTip" Value="{Binding RelativeSource={x:Static RelativeSource.Self}, Path=(Validation.Errors).CurrentItem.ErrorContent}" />                        </Trigger>                        <Trigger SourceName="_expandableButton" Property="Visibility" Value="Visible">                            <Setter Property="Focusable" Value="False" />                            <Setter TargetName="_expandableButtonColumn" Property="ColumnDefinition.MinWidth" Value="15" />                        </Trigger>                        <DataTrigger Value="{x:Static pg:StringConstants.Local}" Binding="{Binding ElementName=_optionsImage, Path=ToolTip}">                            <Setter TargetName="_optionsImage" Property="Image.Source" Value="/BgControls;component/Assets/Images/PropertyGrid/local11.png" />                        </DataTrigger>                        <DataTrigger Value="{x:Static pg:StringConstants.Resource}" Binding="{Binding ElementName=_optionsImage, Path=ToolTip}">                            <Setter TargetName="_optionsImage" Property="Image.Source" Value="/BgControls;component/Assets/Images/PropertyGrid/resource11.png" />                        </DataTrigger>                        <DataTrigger Value="{x:Static pg:StringConstants.Databinding}" Binding="{Binding ElementName=_optionsImage, Path=ToolTip}">                            <Setter TargetName="_optionsImage" Property="Image.Source" Value="/BgControls;component/Assets/Images/PropertyGrid/database11.png" />                        </DataTrigger>                        <DataTrigger Value="{x:Static pg:StringConstants.Inheritance}" Binding="{Binding ElementName=_optionsImage, Path=ToolTip}">                            <Setter TargetName="_optionsImage" Property="Image.Source" Value="/BgControls;component/Assets/Images/PropertyGrid/inheritance11.png" />                        </DataTrigger>                        <DataTrigger Value="{x:Static pg:StringConstants.StyleSetter}" Binding="{Binding ElementName=_optionsImage, Path=ToolTip}">                            <Setter TargetName="_optionsImage" Property="Image.Source" Value="/BgControls;component/Assets/Images/PropertyGrid/style11.png" />                        </DataTrigger>                        <DataTrigger Value="{x:Static pg:StringConstants.Default}" Binding="{Binding ElementName=_optionsImage, Path=ToolTip}">                            <Setter TargetName="_optionsImage" Property="Image.Source" Value="/BgControls;component/Assets/Images/PropertyGrid/advancedproperties11.png" />                        </DataTrigger>                    </ControlTemplate.Triggers>                </ControlTemplate>            </Setter.Value>        </Setter>    </Style>    <Style x:Key="PropertyGridBaseStyle" TargetType="{x:Type pg:PropertyGrid}">        <Setter Property="Background" Value="{DynamicResource ControlNormalBackgroundKey }" />        <Setter Property="BorderBrush" Value="{DynamicResource ControlNormalBorderKey}" />        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />        <Setter Property="FontSize" Value="{DynamicResource TextFontSize}" />        <Setter Property="BorderThickness" Value="1" />        <Setter Property="SelectedObjectType" Value="{Binding Path=SelectedObject, ConverterParameter=Type, RelativeSource={RelativeSource Self}, Converter={StaticResource objectConverter}}" />        <Setter Property="SelectedObjectTypeName" Value="{Binding Path=SelectedObject, ConverterParameter=TypeName, RelativeSource={RelativeSource Self}, Converter={StaticResource objectConverter}}" />        <Setter Property="SelectedObjectName" Value="{Binding Path=SelectedObject, ConverterParameter=SelectedObjectName, RelativeSource={RelativeSource Self}, Converter={StaticResource objectConverter}}" />        <Setter Property="AdvancedOptionsMenu" Value="{StaticResource SelectedObjectAdvancedOptionsMenu}" />        <Setter Property="CategoryGroupHeaderTemplate">            <Setter.Value>                <DataTemplate>                    <TextBlock Text="{Binding}" HorizontalAlignment="Left"/>                </DataTemplate>            </Setter.Value>        </Setter>        <Setter Property="Template">            <Setter.Value>                <ControlTemplate TargetType="{x:Type pg:PropertyGrid}">                    <Border Background="{TemplateBinding Background}"                             BorderBrush="{TemplateBinding BorderBrush}"                             BorderThickness="{TemplateBinding BorderThickness}">                        <Grid Margin="{TemplateBinding Padding}">                            <Grid.RowDefinitions>                                <RowDefinition Height="Auto" />                                <RowDefinition Height="Auto" />                                <RowDefinition />                                <RowDefinition Height="5" />                                <RowDefinition Height="Auto" />                            </Grid.RowDefinitions>                            <!-- 标题栏 -->                            <StackPanel Name="selectObjectNamePanel" Orientation="Horizontal" Visibility="Visible">                                <Border Background="{DynamicResource RegionBrush}"                                         BorderBrush="{DynamicResource BorderBrush}"                                         BorderThickness="1" Width="44" Height="44" Margin="6,4,0,4" Padding="2"                                         Visibility="{Binding ShowPreview, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityConverter}}">                                    <Rectangle>                                        <Shape.Fill>                                            <VisualBrush Stretch="Uniform" Visual="{Binding Path=SelectedObject, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ObjectToUIElementConverter}}" />                                        </Shape.Fill>                                    </Rectangle>                                </Border>                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="6,2,6,4"                                             Visibility="{Binding ShowTitle, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityConverter}}">                                    <Grid MaxWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource HalfConverter}, ConverterParameter=17}">                                        <pg:TrimmedTextBlock Text="{TemplateBinding SelectedObjectTypeName}" TextTrimming="CharacterEllipsis" FontWeight="Bold" AutomationProperties.AutomationId="SelectedObjectTypeName" />                                    </Grid>                                    <Grid Margin="5,0,0,0" MaxWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource HalfConverter}, ConverterParameter=17}">                                        <pg:TrimmedTextBlock Text="{TemplateBinding SelectedObjectName}" TextTrimming="CharacterEllipsis" AutomationProperties.AutomationId="SelectedObjectName" />                                    </Grid>                                </StackPanel>                            </StackPanel>                            <!-- 标题栏 ShowPreview -->                            <ListView x:Name="multiSelectObjectsNamePanel"                                       AutomationProperties.AutomationId="MultiSelectedObjectsNames"                                       AutomationProperties.Name="MultiSelectedObjectsNames"                                      Foreground="{TemplateBinding Foreground}"                                       ItemsSource="{TemplateBinding SelectedObjectsOverride}"                                       Visibility="Collapsed"                                       Background="Transparent"                                       BorderThickness="0" >                                <ItemsControl.ItemTemplate>                                    <DataTemplate>                                        <StackPanel Orientation="Horizontal">                                            <Border x:Name="PreviewBorder"                                                     Visibility="Collapsed"                                                     Background="Transparent"                                                     BorderBrush="{DynamicResource BorderBrush}"                                                     BorderThickness="1"                                                     Width="44" Height="44"                                                     Margin="6,4,0,4"                                                     Padding="2">                                                <Rectangle>                                                    <Shape.Fill>                                                        <VisualBrush Stretch="Uniform" Visual="{Binding ., Converter={StaticResource ObjectToUIElementConverter}}" />                                                    </Shape.Fill>                                                </Rectangle>                                            </Border>                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="6,2,6,4"                                                         Visibility="{Binding ShowTitle, RelativeSource={RelativeSource AncestorType={x:Type pg:PropertyGrid}}, Converter={StaticResource Boolean2VisibilityConverter}}">                                                <Grid MaxWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type pg:PropertyGrid}}, Converter={StaticResource HalfConverter}, ConverterParameter=17}">                                                    <pg:TrimmedTextBlock TextTrimming="CharacterEllipsis" FontWeight="Bold" Text="{Binding ., ConverterParameter=TypeName, Converter={StaticResource objectConverter}}" />                                                </Grid>                                                <Grid Margin="5,0,0,0" MaxWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type pg:PropertyGrid}}, Converter={StaticResource HalfConverter}, ConverterParameter=17}">                                                    <pg:TrimmedTextBlock TextTrimming="CharacterEllipsis" Text="{Binding ., ConverterParameter=SelectedObjectName, Converter={StaticResource objectConverter}}" />                                                </Grid>                                            </StackPanel>                                        </StackPanel>                                        <DataTemplate.Triggers>                                            <DataTrigger Binding="{Binding ShowPreview, RelativeSource={RelativeSource AncestorType={x:Type pg:PropertyGrid}}}" Value="True" >                                                <Setter TargetName="PreviewBorder" Property="Visibility" Value="Visible" />                                            </DataTrigger>                                        </DataTemplate.Triggers>                                    </DataTemplate>                                </ItemsControl.ItemTemplate>                                <ItemsControl.ItemContainerStyle>                                    <Style TargetType="{x:Type ListViewItem}">                                        <Setter Property="Template">                                            <Setter.Value>                                                <ControlTemplate TargetType="{x:Type ListViewItem}">                                                    <ContentPresenter />                                                </ControlTemplate>                                            </Setter.Value>                                        </Setter>                                    </Style>                                </ItemsControl.ItemContainerStyle>                            </ListView>                            <!--工具栏-->                            <Grid x:Name="toolsContainer" Grid.Row="1" Margin="4,0,4,4">                                <Grid.ColumnDefinitions>                                    <ColumnDefinition Width="Auto" />                                    <ColumnDefinition Width="*" />                                </Grid.ColumnDefinitions>                                <Grid Name="sortButtonContainer" Margin="4" Visibility="{Binding ShowSortOptions, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityConverter}}">                                    <StackPanel Orientation="Horizontal" Margin="1">                                        <RadioButton VerticalAlignment="Center" ToolTip="Categorized"                                                      AutomationProperties.AutomationId="CategorizedRadioButton"                                                      AutomationProperties.Name="Categorized"                                                      Style="{StaticResource OptionsToggleButtonStyle}"                                                      IsChecked="{Binding IsCategorized, RelativeSource={RelativeSource TemplatedParent}}">                                            <Image Width="16" Height="16"                                                    Source="/BgControls;component/Assets/Images/PropertyGrid/categorize16.png"                                                    AutomationProperties.AutomationId="CategorizedImage"                                                    AutomationProperties.Name="CategorizedImage" />                                        </RadioButton>                                        <RadioButton VerticalAlignment="Center" ToolTip="Alphabetical"                                                      AutomationProperties.AutomationId="AlphabeticalRadioButton"                                                      AutomationProperties.Name="Alphabetical"                                                      Style="{StaticResource OptionsToggleButtonStyle}"                                                      IsChecked="{Binding IsCategorized, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2BooleanReConverter}}">                                            <Image Width="16" Height="16"                                                    Source="/BgControls;component/Assets/Images/PropertyGrid/sortascending16_white.png"                                                    AutomationProperties.AutomationId="AlphabeticalImage"                                                    AutomationProperties.Name="AlphabeticalImage" />                                        </RadioButton>                                    </StackPanel>                                </Grid>                                <Border Name="searchBoxContainer" Grid.Column="1" Margin="4,4,0,4"                                         Background="{DynamicResource SecondaryRegionBrush}"                                         BorderBrush="{DynamicResource SecondaryBorderBrush}"                                         BorderThickness="1"                                         Visibility="{Binding ShowSearchBox, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityConverter}}">                                    <Grid>                                        <Grid.ColumnDefinitions>                                            <ColumnDefinition Width="*" />                                            <ColumnDefinition Width="Auto" />                                        </Grid.ColumnDefinitions>                                        <local:WatermarkTextBox x:Name="_txtFilter" Background="Transparent"                                                                 AutomationProperties.AutomationId="SearchTextBox"                                                                 AutomationProperties.Name="SearchTextBox"                                                                 BorderThickness="1"                                                                 Watermark="{Binding FilterWatermark, RelativeSource={RelativeSource TemplatedParent}}"                                                                 Text="{Binding Filter,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource TemplatedParent}}">                                            <local:WatermarkTextBox.Style>                                                <Style TargetType="{x:Type local:WatermarkTextBox}" BasedOn="{StaticResource {x:Type local:WatermarkTextBox}}">                                                    <Setter Property="BorderBrush" Value="Transparent" />                                                    <Setter Property="VerticalContentAlignment" Value="Center" />                                                    <Style.Triggers>                                                        <Trigger Property="IsMouseOver" Value="True">                                                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />                                                        </Trigger>                                                    </Style.Triggers>                                                </Style>                                            </local:WatermarkTextBox.Style>                                        </local:WatermarkTextBox>                                        <Button Grid.Column="1" Command="{x:Static commands:PropertyGridCommands.ClearFilter}"                                                 HorizontalAlignment="Left"                                                 AutomationProperties.AutomationId="ClearFilterButton"                                                 AutomationProperties.Name="ClearFilter" Style="{StaticResource ClearFilterButtonStyle}">                                            <Image Source="/BgControls;component/Assets/Images/PropertyGrid/clearfilter16.png" Width="16" Height="16" AutomationProperties.AutomationId="ClearFilterImage" AutomationProperties.Name="ClearFilterImage" />                                        </Button>                                    </Grid>                                </Border>                            </Grid>                            <!--属性列表-->                            <Grid Row="2" Background="Transparent">                                <pg:PropertyItemsControl x:Name="PART_PropertyItemsControl" IsTabStop="False"                                                          Focusable="False"                                                         VirtualizingPanel.VirtualizationMode="Recycling"                                                          VirtualizingPanel.IsVirtualizing="{Binding IsVirtualizing, RelativeSource={RelativeSource TemplatedParent}}"                                                          VerticalAlignment="Top"                                                          ItemsSource="{Binding Properties, RelativeSource={RelativeSource TemplatedParent}}"                                                          ItemContainerStyle="{Binding PropertyContainerStyle, RelativeSource={RelativeSource TemplatedParent}}"                                                          ScrollViewer.CanContentScroll="{Binding IsVirtualizing, RelativeSource={RelativeSource TemplatedParent}}">                                    <pg:PropertyItemsControl.ItemsPanel>                                        <ItemsPanelTemplate>                                            <VirtualizingStackPanel />                                        </ItemsPanelTemplate>                                    </pg:PropertyItemsControl.ItemsPanel>                                    <pg:PropertyItemsControl.GroupStyle>                                        <GroupStyle ContainerStyleSelector="{StaticResource categoryGroupStyleSelector}" />                                    </pg:PropertyItemsControl.GroupStyle>                                    <pg:PropertyItemsControl.Template>                                        <ControlTemplate>                                            <Border BorderThickness="{TemplateBinding BorderThickness}"                                                     Padding="{TemplateBinding Padding}"                                                     BorderBrush="{TemplateBinding BorderBrush}"                                                     Background="{TemplateBinding Background}"                                                     SnapsToDevicePixels="True">                                                <ScrollViewer Name="ScrollBar"                                                               Padding="{TemplateBinding Padding}" Focusable="False"                                                               VerticalScrollBarVisibility="Auto"                                                               HorizontalScrollBarVisibility="Disabled">                                                    <ItemsPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />                                                </ScrollViewer>                                            </Border>                                            <ControlTemplate.Triggers>                                                <DataTrigger Value="True" Binding="{Binding ShowHorizontalScrollBar, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=pg:PropertyGrid}}">                                                    <Setter TargetName="ScrollBar" Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />                                                </DataTrigger>                                            </ControlTemplate.Triggers>                                        </ControlTemplate>                                    </pg:PropertyItemsControl.Template>                                </pg:PropertyItemsControl>                                <Thumb x:Name="PART_DragThumb" Margin="6,0,0,0" Width="5"                                       HorizontalAlignment="Left"                                                                                                                       Cursor="SizeWE"                                        AutomationProperties.AutomationId="VerticalThumb"                                        AutomationProperties.Name="VerticalThumb">                                    <Control.Template>                                        <ControlTemplate>                                            <Border Background="Transparent" />                                        </ControlTemplate>                                    </Control.Template>                                </Thumb>                            </Grid>                            <!--分割线-->                            <GridSplitter Height="3" Grid.Row="3" HorizontalAlignment="Stretch"                                           AutomationProperties.AutomationId="SummaryThumb"                                           AutomationProperties.Name="SummaryThumb"                                           Background="{DynamicResource SecondaryRegionBrush}"                                          Visibility="{Binding ShowSummary, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityConverter}}" />                            <!--属性描述-->                            <StackPanel Grid.Row="4" Margin="0,0,0,5"                                         Visibility="{Binding ShowSummary, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource Boolean2VisibilityConverter}}">                                <TextBlock Padding="2 2 2 0"                                            FontWeight="Bold"                                            HorizontalAlignment="Left"                                           AutomationProperties.AutomationId="PropertyItemDisplayName"                                            Text="{Binding SelectedPropertyItem.DisplayName, RelativeSource={RelativeSource TemplatedParent}}" />                                <TextBlock Padding="5 2 2 0"                                            HorizontalAlignment="Left"                                           TextWrapping="Wrap"                                            AutomationProperties.AutomationId="PropertyItemDescription"                                            Text="{Binding SelectedPropertyItem.Description, RelativeSource={RelativeSource TemplatedParent}}" />                            </StackPanel>                        </Grid>                    </Border>                    <ControlTemplate.Triggers>                        <MultiTrigger>                            <MultiTrigger.Conditions>                                <Condition SourceName="searchBoxContainer" Property="Visibility" Value="Collapsed" />                                <Condition SourceName="sortButtonContainer" Property="Visibility" Value="Collapsed" />                            </MultiTrigger.Conditions>                            <Setter TargetName="toolsContainer" Property="Visibility" Value="Collapsed" />                        </MultiTrigger>                        <MultiDataTrigger>                            <MultiDataTrigger.Conditions>                                <Condition Value="False" Binding="{Binding SelectedObjectsOverride, RelativeSource={RelativeSource Self}, Converter={StaticResource Null2BoolConverter}}" />                                <Condition Value="True" Binding="{Binding ShowTitle, RelativeSource={RelativeSource Self}}" />                            </MultiDataTrigger.Conditions>                            <Setter TargetName="multiSelectObjectsNamePanel" Property="Visibility" Value="Visible" />                            <Setter TargetName="selectObjectNamePanel" Property="Visibility" Value="Collapsed" />                        </MultiDataTrigger>                    </ControlTemplate.Triggers>                </ControlTemplate>            </Setter.Value>        </Setter>    </Style>    <Style TargetType="{x:Type pg:PropertyItem}" BasedOn="{StaticResource PropertyItemBaseStyle}" />    <Style TargetType="{x:Type pg:PropertyGrid}" BasedOn="{StaticResource PropertyGridBaseStyle}" /></ResourceDictionary>