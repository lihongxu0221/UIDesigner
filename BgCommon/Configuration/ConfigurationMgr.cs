namespace BgCommon.Configuration;/// <summary>/// 配置类，用于保存特定实体类型的设置./// </summary>/// <typeparam name="TEntity">实体类型.</typeparam>[Serializable]public class ConfigurationMgr<TEntity> : ObservableObject    where TEntity : class, new(){    [NonSerialized]    private static readonly JsonSerializerOptions Options = new() { WriteIndented = true }; // 美化输出，方便阅读    /// <summary>    /// 序列化方法枚举.    /// </summary>    public enum SerializeMethod    {        /// <summary>        /// 使用二进制进行序列化.        /// </summary>        Bin,        /// <summary>        /// 使用XML进行序列化.        /// </summary>        Xml,        /// <summary>        /// 使用 System.Text.Json 进行序列化.        /// </summary>        Json,    }    // 私有字段.    [NonSerialized]    private bool hasLoadAll = false;    [NonSerialized]    private string filePath = string.Empty;    private TEntity? entity = new();    [NonSerialized]    private SerializeMethod method = SerializeMethod.Bin;    /// <summary>    /// Gets or sets 获取或设置配置的实体实例.    /// </summary>    [Browsable(true)]    public TEntity? Entity    {        get { return this.entity; }        set { _ = this.SetProperty(ref this.entity, value); }    }    /// <summary>    /// Gets or sets a value indicating whether 获取或设置一个值，该值指示是否已加载所有配置.    /// </summary>    [Browsable(false)]    public bool HasLoadAll    {        get { return this.hasLoadAll; }        set { _ = this.SetProperty(ref this.hasLoadAll, value); }    }    /// <summary>    /// Gets 获取配置的序列化方法.    /// </summary>    [Browsable(false)]    public SerializeMethod Method    {        get { return this.method; }        private set { _ = this.SetProperty(ref this.method, value); }    }    /// <summary>    /// Gets 获取配置文件的完整路径.    /// </summary>    [Browsable(false)]    public string FilePath    {        get { return this.filePath; }    }    /// <summary>    /// Gets 获取配置文件的根目录路径.    /// </summary>    [Browsable(false)]    public string? RootDirPath    {        get { return Path.GetDirectoryName(this.filePath); }    }    /// <summary>    /// Gets 获取配置文件的文件名.    /// </summary>    [Browsable(false)]    public string FileName    {        get { return Path.GetFileName(this.filePath); }    }    /// <summary>    /// Gets a value indicating whether 该值指示配置是否为空（即实体为null）.    /// </summary>    public bool IsEmpty => this.Entity == null;    /// <summary>    /// Initializes a new instance of the <see cref="ConfigurationMgr{TEntity}"/> class.    /// 使用此构造函数时，需要后续调用方法. <code>SaveToFile(entity, filePath)</code> 来设置并保存配置.    /// </summary>    /// <param name="method">序列化方法.</param>    public ConfigurationMgr(SerializeMethod method = SerializeMethod.Bin)    {        this.Method = method;    }    /// <summary>    /// Initializes a new instance of the <see cref="ConfigurationMgr{TEntity}"/> class.    /// </summary>    /// <param name="filePath">配置文件的路径.</param>    /// <param name="method">序列化方法.</param>    public ConfigurationMgr(string filePath, SerializeMethod method = SerializeMethod.Bin)        : this(filePath, null, method)    {    }    /// <summary>    /// Initializes a new instance of the <see cref="ConfigurationMgr{TEntity}"/> class.    /// </summary>    /// <param name="filePath">配置文件的路径.</param>    /// <param name="entity">实体实例.</param>    /// <param name="method">序列化方法.</param>    public ConfigurationMgr(string filePath, TEntity? entity, SerializeMethod method = SerializeMethod.Bin)    {        this.Method = method;        this.Entity = entity;        this.SetFilePath(filePath);    }    /// <summary>    /// 根据指定的序列化方法从文件加载配置.如果文件不存在，则会创建一个新的实体实例.    /// </summary>    /// <exception cref="NotSupportedException">当序列化方法不受支持时抛出.</exception>    public bool LoadFromFile()    {        if (!File.Exists(this.filePath))        {            this.Entity = new TEntity();            return false; // 文件不存在，直接返回新创建的实例.        }        // 根据序列化方法实现加载逻辑.        switch (this.Method)        {            case SerializeMethod.Bin: // 加载二进制序列化.                {                    using var fileStream = new FileStream(this.filePath, FileMode.Open, FileAccess.Read, FileShare.Read);                    var formatter = new BinaryFormatter();                    this.Entity = (TEntity?)formatter.Deserialize(fileStream);                    break;                }            case SerializeMethod.Xml: // 加载XML序列化.                {                    using var fileStream = new FileStream(this.filePath, FileMode.Open, FileAccess.Read, FileShare.Read);                    System.Xml.Serialization.XmlSerializer serializer = new(typeof(TEntity));                    this.Entity = (TEntity?)serializer.Deserialize(fileStream);                    break;                }            case SerializeMethod.Json: // 加载JSON序列化.                {                    using var fileStream = new FileStream(this.filePath, FileMode.Open, FileAccess.Read, FileShare.Read);                    using var reader = new StreamReader(fileStream);                    string json = reader.ReadToEnd();                    this.Entity = JsonSerializer.Deserialize<TEntity>(json);                    break;                }            default:                throw new NotSupportedException($"序列化方法 {this.Method} 不受支持.");        }        return true;    }    /// <summary>    /// 从文件异步加载配置.如果文件不存在，则会创建一个新的实体实例.    /// </summary>    /// <param name="cancellationToken">用于取消操作的令牌.</param>    /// <returns>Task.</returns>    public async Task<bool> LoadFromFileAsync(CancellationToken cancellationToken = default)    {        if (!File.Exists(this.filePath))        {            this.Entity = new TEntity();            return false;        }        switch (this.Method)        {            case SerializeMethod.Bin: // 加载二进制序列化.                // BinaryFormatter 没有异步API，但我们可以在后台线程中执行以避免阻塞UI.                // 为了简单起见，这里仍使用同步方式，实际应用可考虑 Task.Run.                using (FileStream fileStream = new(this.filePath, FileMode.Open, FileAccess.Read, FileShare.Read))                {                    BinaryFormatter formatter = new();                    this.Entity = (TEntity?)formatter.Deserialize(fileStream);                }                break;            case SerializeMethod.Xml: // XmlSerializer 没有内置的异步反序列化，但我们可以异步读取文件.                string xmlContent = await File.ReadAllTextAsync(this.filePath, cancellationToken);                using (var reader = new StringReader(xmlContent))                {                    var serializer = new System.Xml.Serialization.XmlSerializer(typeof(TEntity));                    this.Entity = (TEntity?)serializer.Deserialize(reader);                }                break;            case SerializeMethod.Json: // 使用 System.Text.Json 的异步API.                using (var fileStream = new FileStream(this.filePath, FileMode.Open, FileAccess.Read, FileShare.Read))                {                    this.Entity = await JsonSerializer.DeserializeAsync<TEntity>(fileStream, cancellationToken: cancellationToken);                }                break;            default:                throw new NotSupportedException($"序列化方法 {this.Method} 不受支持.");        }        return true;    }    /// <summary>    /// 根据指定的序列化方法将当前配置保存到文件.    /// </summary>    public void SaveToFile()    {        // 调用静态方法来执行实际的保存操作.        Save(this.Entity, this.FilePath, this.Method);    }    /// <summary>    /// 根据指定的序列化方法将当前配置异步保存到文件.    /// </summary>    /// <param name="cancellationToken">用于取消操作的令牌.</param>    /// <returns>表示异步保存操作的任务.</returns>    public Task SaveToFileAsync(CancellationToken cancellationToken = default)    {        return this.SaveAsync(this.Entity, this.FilePath, this.Method, cancellationToken);    }    /// <summary>    /// 将指定的实体保存到文件，并更新当前实例的实体和文件路径.    /// </summary>    /// <param name="entity">要保存的实体实例.</param>    /// <param name="filePath">文件的完整路径，如果为null则使用实例当前的路径.</param>    /// <exception cref="ArgumentNullException">当实体或文件路径为空时抛出.</exception>    /// <exception cref="NotSupportedException">当序列化方法不受支持时抛出.</exception>    public void SaveToFile(TEntity? entity, string? filePath = null)    {        this.Entity = entity;        if (!string.IsNullOrEmpty(filePath) && this.FilePath != filePath)        {            this.SetFilePath(filePath);        }        this.Save(this.Entity, this.FilePath, this.Method);    }    /// <summary>    /// 静态工具方法，用于将实体保存到指定的文件路径.    /// </summary>    /// <param name="entity">要保存的实体.</param>    /// <param name="filePath">文件路径.</param>    /// <param name="method">序列化方法.</param>    public void Save(TEntity? entity, string filePath, SerializeMethod method)    {        ArgumentNullException.ThrowIfNull(entity, nameof(entity));        ArgumentNullException.ThrowIfNull(filePath, nameof(filePath));        // 确保目录存在.        string? directory = Path.GetDirectoryName(filePath);        if (!string.IsNullOrEmpty(directory) && !Directory.Exists(directory))        {            _ = Directory.CreateDirectory(directory);        }        string tempFilePath = $"{filePath}.tmp";        try        {            switch (method)            {                case SerializeMethod.Bin:                    using (FileStream fileStream = new(tempFilePath, FileMode.Create, FileAccess.Write, FileShare.None))                    {                        BinaryFormatter formatter = new();                        fileStream.Seek(0, SeekOrigin.Begin);                        formatter.Serialize(fileStream, entity);                        fileStream.Flush(true);                    }                    break;                case SerializeMethod.Xml:                    using (FileStream fileStream = new(tempFilePath, FileMode.Create, FileAccess.Write, FileShare.None))                    {                        var serializer = new System.Xml.Serialization.XmlSerializer(typeof(TEntity));                        fileStream.Seek(0, SeekOrigin.Begin);                        serializer.Serialize(fileStream, entity);                        fileStream.Flush(true);                    }                    break;                case SerializeMethod.Json:                    string json = JsonSerializer.Serialize(entity, Options);                    File.WriteAllText(tempFilePath, json);                    break;                default:                    throw new NotSupportedException($"序列化方法 {method} 不受支持.");            }            // 原子性替换目标文件            if (File.Exists(filePath))            {                File.Delete(filePath);            }            File.Move(tempFilePath, filePath);        }        catch (NotSupportedException)        {            throw;        }        catch        {            // 发生异常时删除可能残留的临时文件            if (File.Exists(tempFilePath))            {                File.Delete(tempFilePath);            }            throw;        }    }    /// <summary>    /// 静态工具方法，用于将实体异步保存到指定的文件路径.    /// </summary>    /// <param name="entity">要保存的实体.</param>    /// <param name="filePath">文件路径.</param>    /// <param name="method">序列化方法.</param>    /// <param name="cancellationToken">用于取消操作的令牌.</param>    /// <returns>表示异步保存操作的任务.</returns>    public async Task SaveAsync(TEntity? entity, string filePath, SerializeMethod method, CancellationToken cancellationToken = default)    {        ArgumentNullException.ThrowIfNull(entity, nameof(entity));        ArgumentNullException.ThrowIfNull(filePath, nameof(filePath));        // 确保目录存在.        string? directory = Path.GetDirectoryName(filePath);        if (!string.IsNullOrEmpty(directory) && !Directory.Exists(directory))        {            _ = Directory.CreateDirectory(directory);        }        string tempFilePath = $"{filePath}.tmp";        try        {            switch (method)            {                case SerializeMethod.Bin:                    {                        using var fileStream = new FileStream(tempFilePath, FileMode.Create, FileAccess.Write, FileShare.None);                        var formatter = new BinaryFormatter();                        fileStream.Seek(0, SeekOrigin.Begin);                        formatter.Serialize(fileStream, entity);                        fileStream.Flush(true);                        break;                    }                case SerializeMethod.Xml:                    // XmlSerializer 没有内置的异步序列化，但我们可以异步写入文件.                    {                        using var memoryStream = new MemoryStream();                        var serializer = new System.Xml.Serialization.XmlSerializer(typeof(TEntity));                        serializer.Serialize(memoryStream, entity);                        memoryStream.Position = 0;                        using var fileStream = new FileStream(tempFilePath, FileMode.Create, FileAccess.Write, FileShare.None, 4096, true);                        await memoryStream.CopyToAsync(fileStream, cancellationToken);                        break;                    }                case SerializeMethod.Json:                    {                        using var fileStream = new FileStream(tempFilePath, FileMode.Create, FileAccess.Write, FileShare.None, 4096, true);                        await JsonSerializer.SerializeAsync(fileStream, entity, Options, cancellationToken);                        break;                    }                default:                    throw new NotSupportedException($"序列化方法 {method} 不受支持.");            }            // 原子性替换目标文件            if (File.Exists(filePath))            {                File.Delete(filePath);            }            File.Move(tempFilePath, filePath);        }        catch (NotSupportedException)        {            throw;        }        catch        {            // 发生异常时删除可能残留的临时文件            if (File.Exists(tempFilePath))            {                File.Delete(tempFilePath);            }            throw;        }    }    /// <summary>    /// 设置配置文件的完整路径，并确保路径和目录有效.    /// </summary>    /// <param name="filePath">配置文件所在的完整路径.</param>    private void SetFilePath(string filePath)    {        // 校验文件路径是否为空.        ArgumentNullException.ThrowIfNullOrEmpty(filePath);        string? directory = Path.GetDirectoryName(filePath);        string? fileName = Path.GetFileName(filePath);        // 文件名不能为空.        ArgumentNullException.ThrowIfNullOrEmpty(fileName, nameof(filePath));        if (string.IsNullOrEmpty(directory))        {            // 如果未指定目录，则默认为当前应用程序的基目录.            directory = AppDomain.CurrentDomain.BaseDirectory;            filePath = Path.Combine(directory, fileName);        }        if (!Path.IsPathRooted(directory))        {            // 如果是相对路径，则转换为基于应用程序基目录的绝对路径.            directory = Path.Combine(AppDomain.CurrentDomain.BaseDirectory, directory);            filePath = Path.Combine(directory, fileName);        }        // 确保目录存在.        if (!Directory.Exists(directory))        {            _ = Directory.CreateDirectory(directory);        }        this.filePath = filePath;    }}